<?php 
    $setores = ['Financeiro', 'Acadêmico', 'Interno'];
        function listar_setores($lista) {
            foreach($lista as $setor){
                echo "<option value = $setor>$setor</option>";
            }
        } //Você Precisa transferir isso para aba de setores
        function adicionar_setor(){
            ;
        }
